"""Knowledge Graph router placeholder."""

from fastapi import APIRouter

graph_router = APIRouter(prefix="/graph", tags=["Knowledge Graph"])

# TODO: Add Phase 5 Knowledge Graph Endpoints:
# - GET /graph (Fetch graph nodes and relationships)
